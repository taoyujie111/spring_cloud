
server:
  port: 8010
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8800/eureka/
  instance:
    hostname: localhost

spring:
  application:
    name: base-provider