
server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8800/eureka
  instance:
    hostname: localhost

spring:
  application:
    name: base-consumer